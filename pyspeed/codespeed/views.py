# -*- coding: utf-8 -*-
from django.shortcuts import render_to_response

#def revision_info(request):
    #filter(interpreter__startswith="pypy")
    #return render_to_response('current_datetime.html', {'current_date': now})